# Core PDF
pikepdf>=8.7.0
pymupdf>=1.24.0
pypdf>=4.0.0
pdfplumber>=0.10.4
# GUI — Essentials only. The app imports QtCore, QtGui and QtWidgets and
# nothing else, while the full PySide6 metapackage drags in Qt WebEngine,
# which alone is over 500 MB in the built bundle.
#
# Build with Python 3.11: the Qt platform plugins fail to load under the
# Python 3.9 that ships with Xcode on Apple Silicon, which leaves the app
# unable to start at all.
PySide6-Essentials>=6.7.0,<6.9.0
# Image processing
Pillow>=10.2.0
# OCR (optional - downloads tessdata at runtime)
pytesseract>=0.3.10
# Conversion: HTML -> PDF
weasyprint>=62.3
# Conversion: Word <-> PDF
python-docx>=1.1.0
docx2pdf>=0.1.8 ; sys_platform == "darwin"
# Conversion: Excel
openpyxl>=3.1.2
# Conversion: PPT
python-pptx>=0.6.23
# PDF repair
# (uses pikepdf which can salvage many damaged PDFs)
# Build/packaging
py2app>=0.28 ; sys_platform == "darwin"
